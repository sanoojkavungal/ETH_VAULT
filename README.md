# ETH_VAULT
ETH Vault is a decentralized application that allows users to deposit ETH with a time lock and withdraw it only after the lock period expires, built using Solidity, ethers.js, and MetaMask on the Sepolia testnet.
